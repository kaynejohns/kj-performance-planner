PORT=8787
# Optional: remote JSON refinement for detailed plans (same schema as the structured base plan)
PERFORMANCE_PLANNER_API_KEY=
PERFORMANCE_PLANNER_MODEL=gpt-4o-mini
VITE_API_BASE_URL=/api/performance-planner
