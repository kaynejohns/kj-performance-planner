[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
  # firebase-admin and pdfkit can't be bundled by esbuild — keep them as external
  external_node_modules = ["firebase-admin", "pdfkit", "@google-cloud/firestore", "google-auth-library"]
  [functions."api"]
    timeout = 26

# API routes → serverless function
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200

# React planner SPA — serve planner.html for all /planner/* routes
[[redirects]]
  from = "/planner/*"
  to = "/planner.html"
  status = 200

[[redirects]]
  from = "/planner"
  to = "/planner.html"
  status = 200

# Root → landing page (already public/index.html, served by default)
